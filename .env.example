# Copy to .env (locally) or set on Vercel Project Settings → Environment Variables
# Required
ABLY_API_KEY=your-ably-api-key:xxxxxxxxxxxxxxxxx
# Optional – only if you later add Vercel KV (not required for this starter)
KV_REST_API_URL=
KV_REST_API_TOKEN=
